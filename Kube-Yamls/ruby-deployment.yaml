apiVersion: apps/v1
kind: Deployment
metadata:
  name: ruby-BUILDNO
  labels:
    app: ruby
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ruby
  template:
    metadata:
      labels:
        app: ruby
    spec:
      volumes:
      - name: db-volume
        configMap:
          name: db-config
      containers:
      - name: ruby
        image: DIMG
        ports:
        - containerPort: 3000
        volumeMounts:
          - mountPath: /config/
            name: db-volume